name: shoppingo
description: A new Flutter project.
publish_to: "none"

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  shared_preferences: ^2.1.0
  photo_view: ^0.14.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.14.1

flutter_launcher_icons:
  android: "launcher_icon" 
  ios: true               
  image_path: "assets/icon/icon.png" 
  
flutter:
  uses-material-design: true
  # assets:           # 나중에 리소스 쓰면 주석 해제하고 경로를 추가
  #   - assets/

